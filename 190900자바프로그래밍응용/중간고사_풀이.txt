#3
Member b=new Member();
		m.birthPlace=new Place();
		m.birthPlace.city="부산";

#4
User
this.email=email;
String
return email+""; //아니면 int를 스트링으로바꾸는 메소드사용

//생선자는 return,void없고 클래스이름이랑똑같게

#5
extends Person

//id필드있으니까 따로안만드어도 됨

Student
super(id); //Person(); -앗!기본생성자가 없다!
//this.id=id; //위에 super 있어서 생략 가능
this.name=name;

#6
Class Num {
	(public) static double PI=3.14;
}

#7
//extends Object
Dog d=new Dog();
d.id="Dog1";
Cat c=new Cat();
c.id="Cat1;
Object v[]={d,c};
